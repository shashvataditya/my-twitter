<%= form_for(@newTweet) do |f| %>
	<center>
	  <div class="field">
	    <%= f.label :content, 'Tweet Content :'%><br />
	    <%= f.text_field :content, autofocus: true %>
	  </div>

	  <div class="actions">
	    <%= f.submit "Post Tweet" %>
	  </div>
	</center>
<% end %>